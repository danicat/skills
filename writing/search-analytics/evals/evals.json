{
  "skill": "search-analytics",
  "version": "0.1.0",
  "evals": [
    {
      "id": "eval-auth",
      "description": "Start OAuth 2.0 web companion to authorize Google Search Console access",
      "prompt": "Authorize my Google Search Console account for search analytics",
      "expected_actions": [
        "python3 scripts/search_analytics.py auth"
      ]
    },
    {
      "id": "eval-sync-incremental",
      "description": "Run incremental sync to ingest recent search performance data into SQLite",
      "prompt": "Sync the latest Google Search Console data into SQLite",
      "expected_actions": [
        "python3 scripts/search_analytics.py sync"
      ]
    },
    {
      "id": "eval-sync-full",
      "description": "Run full 16-month historical backfill into SQLite",
      "prompt": "Backfill all my historical Search Console data from the last 16 months",
      "expected_actions": [
        "python3 scripts/search_analytics.py sync --full"
      ]
    },
    {
      "id": "eval-report-overview",
      "description": "Run the overview analytical report",
      "prompt": "Show me a search performance overview report for my sites",
      "expected_actions": [
        "python3 scripts/search_analytics.py report overview"
      ]
    },
    {
      "id": "eval-sql-query",
      "description": "Execute ad-hoc SQL query against search performance data in SQLite",
      "prompt": "Query my top 10 search queries with more than 100 clicks using SQL",
      "expected_actions": [
        "python3 scripts/search_analytics.py query \"SELECT query, total_clicks, total_impressions, avg_ctr_pct FROM v_top_queries WHERE total_clicks > 100 ORDER BY total_clicks DESC LIMIT 10\""
      ]
    }
  ]
}
